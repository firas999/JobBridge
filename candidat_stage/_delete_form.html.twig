<form method="post" action="{{ path('candidat_stage_delete', {'id': candidat_stage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ candidat_stage.id) }}">
    <button class="btn">Supprimer</button>
</form>
