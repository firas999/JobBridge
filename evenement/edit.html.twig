{% extends 'base.html.twig' %}

{% block title %}Modifier l'évenement{% endblock %}

{% block body %}
    <h1>Modifier l'évenement</h1>

    {{ include('evenement/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('evenement_index') }}">Retourner à la liste des évenements</a>

    {{ include('evenement/_delete_form.html.twig') }}
{% endblock %}
