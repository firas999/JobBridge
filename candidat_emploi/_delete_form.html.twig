<form method="post" action="{{ path('candidat_emploi_delete', {'id': candidat_emploi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ candidat_emploi.id) }}">
    <button class="btn">supprimer</button>
</form>
